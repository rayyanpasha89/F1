# Adversarial repository review

Review date: 2026-09-11. Conducted by the implementing Codex agent, not an independent reviewer. This is an evidence-backed self-review, not a faculty or independent security certification.

| Area | Evidence inspected | Finding / disposition |
|---|---|---|
| Target/future leakage | `backend/ml/features.py`, mutation tests, split masks | Outcomes are scoring columns only; feature allowlists select inputs. Same-date rows are emitted before any history update. Train 2010–2018, selection 2019–2021, final 2022–2024; parameters frozen before final evaluation. |
| Rolling semantics | Driver/team/circuit histories and tests | Previous five appearances/races, fixed debut priors. Team aggregates are race-level means across drivers. Career warmup starts 1990, not all history. |
| Point-in-time provenance | CSV audit and inference entry construction | Results snapshot supplies historical grids and participants; original pre-race publication timestamps and correction history are unavailable. This is retrospective pre-race-feature evaluation, not a verified live forecast archive. |
| Predictions and explanations | Saved artifact pipeline, final report, SHAP reconstruction | Real sklearn inference. Calibrated SHAP base and factors reconstruct probabilities within 1e-7. Factors are associative log-odds, not causal explanations. |
| Race probability coherence | `backend/ml/probability.py`, 128-race evaluation, property and mutation tests | A deterministic clipped-logit projection makes each supported race sum to three within floating-point tolerance and preserves ranking. It constrains marginal probabilities and does not model a joint podium permutation. |
| Artifact lineage and public evidence | Manifest hashes, pre-deserialization verifier, readiness, model-card contract and tamper tests | Runtime binds two joblib artifacts and four reports to one version and fails closed on byte or metadata disagreement. Public output is a strict allowlist. The new analysis reuses consumed 2022–2024 outcomes and is labeled post-test iterative, not unseen evidence. |
| Forecast review boundary | Review service call order, same-race/future mutation tests, Monaco contract | Forecasts and factors are generated before results are read. Recorded labels are joined afterward for whole-grid Brier/absolute error and surprises; result mutation cannot change the forecast payload. |
| Grid scenario boundary | Strict request/response models, predictor copy isolation, symmetry, pit-lane proxy and API tests | Callers can select only two distinct race starters. The service swaps their copied `grid_position` inputs, holds every other value fixed, and preserves the released forecast. Actual positions remain separate from transformed inputs, including grid `0` versus proxy `25`. Both whole-field probability sets sum to three. The response states that the result is non-causal, not newly validated, and not a live-race recommendation. |
| Metrics and hypotheses | EXPERIMENTS, saved reports, executed notebooks | No fabricated successes: probability scores improve, top-three hit rate slightly declines; H3 recent form alone does not beat career. Four notebooks executed from production modules. |
| Frontend data | API client, browser network paths and visible rows | Real API payloads throughout. Fixtures are confined to tests. Browser verified standings, race data, comparisons, profiles, predictions, spelling-aware chat, visible SQL and result charts. |
| SQL mutation boundary | AST validator, SQLite authorizer, live PostgreSQL role test | Single read-only statement, known tables/functions, capped rows and timed execution. SELECT-only PostgreSQL role independently denies DELETE/CREATE. |
| Raw detail policy | Aggregate scope inspection | Found scalar/window aggregate disguises; corrected and covered by three regression cases. Replayed all 30 saved Run 05 SQL queries with unchanged results after the fix. |
| Grounding/hallucination | Bounded router/generator, query augmentation, DB entity/schema retrieval, spelling resolver and live suites | Weather/tyres/fuel/telemetry refused; factual responses render from rows/model. Misspellings resolve only against stored labels, corrections remain visible, and ambiguous historical names require clarification. Browser QA found an overlapping `Jackie Lewis` reference in the trace despite the correct answer; a failing catalog regression now suppresses lower-confidence overlapping components/references when a full name is explicit. Model-supplied assumptions remain unverified and labeled. No manual statistical answer mapping. |
| Benchmark strength | Nine NL2SQL reports, Agentic Run 01, comparison implementation | Run 09 passed 40/40 with 3.651-second mean latency; Runs 07 and 08 preserve distinct 39/40 schema-path failures. Agentic Run 01 passed 22/22 at 3.013 seconds. Row ordering remains unscored; table/key presence checks are proxies. Both suites were used during development and do not establish unseen generalization. |
| Secrets | Git/worktree scans plus direct Lightsail and exported CloudWatch readback | Actual configured Bedrock and database values remain absent from tracked source. `.env`, state and model binaries remain ignored. Five configured sensitive/provider values and credential, database URL, SQL and exact typo-probe question markers had zero matches across 264 bounded direct events and the 210-event CloudWatch snapshot. Runtime secrets never enter Terraform state or release reports. |
| Tests/CI | Full local suites, isolated PostgreSQL and GitHub run 34521407938 | 154 Python tests pass with four opt-in PostgreSQL checks skipped only in the default run; those four pass separately using the dedicated `SELECT`-only role. Fifteen frontend tests, Ruff, frontend lint/format/build, dependency audits and both Terraform roots pass. CI without private CSV/model artifacts exercises a smaller scope; it is not equivalent to full data/model verification. |
| Deployment contracts | ECS secret names versus chat config | Corrected mismatched read-only URL variable before service startup. Added contract coverage. |
| Container | CodeBuild `291889a4-bf82-4c42-b77a-a73a2b4a6a8e`, immutable image `91382f8` | Linux image built the React app, installed locked Python dependencies and verified/deserialized the manifest-bound model artifacts before ECR push. ECR digest `sha256:be81cc251318219892e77d0f381a46de14e0c4d509474f334b8e5aae0e738d8e` runs as Lightsail deployment 13. |
| AWS boundaries | Provider allowlist, private connectivity probe, deployed environment | Confirmed account/default profile only. PostgreSQL remained private during ingestion and runtime. TLS uses `verify-full`; app startup independently rejects writable database credentials. No AWS access keys are in the container. |
| Cloud flow | Append-only public verifier, direct Chromium flows, Lighthouse and runtime logs | Deployment 13 passed 11 public responses including readiness, model card, a 20-driver Monaco forecast summing to three, a 3/3 review and typo correction with two provider calls and zero repair. Desktop/mobile archive, Model Lab and race flows had zero console errors, failed requests or overflow. All audited routes scored 100 accessibility and best practices; maximum final CLS was 0.003297. |
| HTTP and release verification | Middleware tests, production-style probes and final public report | Request IDs are generated rather than reflected; security headers, cache sensitivity, immutable hashed assets and gzip are asserted. The verifier rejects reuse and incoherent probabilities and emits only bounded scalar evidence. `reports/model_accountability_aws_release_final.json` and its SHA-256 are bound into the combined cloud report. |
| Dependency maintenance | Runtime lock, npm lock, CI workflow and Dependabot configuration | CI audits the fully pinned runtime set with pip-audit 2.9.0 and production npm dependencies at high severity. Monthly update PRs are capped for reviewability. An audit is a known-vulnerability check, not proof that dependencies are defect-free. |
| Operational limitations | Terraform, Lightsail, chat limiter, docs | Single small container and micro database; local Terraform state; no durable chat quota or alarm notification subscriber. Lightsail deployment metadata exposes runtime environment values to authorized operators. Earlier ALB/supporting resources and active Lightsail remain billable without traffic. |
| Reproducibility | README, source checksums, dependency locks, Git history | Clone requires exact source package; no redistribution license was supplied. Trusted artifacts reproduced by scripts; no backdating/force-push. Worklog records one earlier test-command fall-through and corrective commit honestly. |

No known implementation or release gate remains for the approved historical-data scope. The model-accountability release is the active immutable deployment and has synchronized local, PostgreSQL, CI, CodeBuild, public-contract, browser, Lighthouse and runtime-log evidence. Faculty review may still identify defects, and the project intentionally excludes weather, live feeds, betting and simulations. Cloud availability and Bedrock behavior remain external operational dependencies.
